

@{
    Layout = "~/Views/Manage/Index.cshtml";

}

<div id="PaymentManagement" class="card">
    <div class="container">
        <div>
            <h3>Odenis sehifesi</h3>
            helelik burda olanlar odenisi imitasiya etmek ucundu.....
        </div>
        @using (Html.BeginForm("AddToBalance", "Payment", FormMethod.Post,
                             new { enctype = "multipart/form-data" }))
        {
            @Html.AntiForgeryToken()
            <div class="form-group">
                <div class="col-md-5">
                    <input type="number" name="amount" required min="0.01" step="0.01" class="form-control">
                    <br />
                </div>
            </div>
            <div class="form-group">
                <button type="submit" class="btn btn-primary"> A D D </button>
            </div>
                }
            </div>
        </div>
